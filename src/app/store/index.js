import { createStore } from 'vuex'
import {booksModule} from '@/entities'

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    booksModule,
  }
})
